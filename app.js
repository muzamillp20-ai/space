const imageBase = 'https://images-assets.nasa.gov/image/';
const objects = [
  { id: 'mercury', name: 'Mercury', category: 'Planets', type: 'Terrestrial planet', meta: '57.9M km from Sun', image: imageBase + 'PIA15162/PIA15162~orig.jpg', color: '#8f8f8f', facts: { diameter: '4,879 km', mass: '3.30 × 10²³ kg', gravity: '3.7 m/s²', temperature: '167°C avg.', distance: '57.9M km', day: '59 Earth days', year: '88 Earth days', moons: '0' }, description: 'The smallest planet and the swiftest world in the Solar System. Mercury races around the Sun in just 88 Earth days.', composition: 'A rocky world with a large metallic core, Mercury is unusually dense for its size.', atmosphere: 'Its extremely thin exosphere is made from atoms blasted off the surface by solar wind and micrometeorites.', moons: 'Mercury travels alone: its intense proximity to the Sun makes it difficult for a natural satellite to remain stable.' },
  { id: 'venus', name: 'Venus', category: 'Planets', type: 'Terrestrial planet', meta: '108.2M km from Sun', image: imageBase + 'PIA00271/PIA00271~orig.jpg', color: '#d09d5d', facts: { diameter: '12,104 km', mass: '4.87 × 10²⁴ kg', gravity: '8.9 m/s²', temperature: '464°C avg.', distance: '108.2M km', day: '243 Earth days', year: '225 Earth days', moons: '0' }, description: 'A world of crushing pressure and runaway heat, Venus is Earth’s near-sized but radically different twin.', composition: 'A silicate mantle surrounds an iron core beneath a volcanic crust.', atmosphere: 'A thick carbon dioxide atmosphere and sulfuric acid clouds trap heat in the Solar System’s strongest greenhouse effect.', moons: 'Venus has no moons.' },
  { id: 'earth', name: 'Earth', category: 'Planets', type: 'Terrestrial planet', meta: '149.6M km from Sun', image: imageBase + 'PIA18033/PIA18033~orig.jpg', color: '#6fb6c5', facts: { diameter: '12,742 km', mass: '5.97 × 10²⁴ kg', gravity: '9.8 m/s²', temperature: '14°C avg.', distance: '149.6M km', day: '23.9 hours', year: '365.25 days', moons: '1' }, description: 'Our home planet is the only known world with liquid oceans on its surface and life in its atmosphere.', composition: 'A solid iron inner core, liquid outer core, rocky mantle and thin crust create a dynamic living world.', atmosphere: 'Nitrogen and oxygen dominate a protective atmosphere that moderates temperature and shields life from radiation.', moons: 'The Moon stabilizes Earth’s tilt and drives the tides.' },
  { id: 'mars', name: 'Mars', category: 'Planets', type: 'Terrestrial planet', meta: '227.9M km from Sun', image: imageBase + 'PIA00407/PIA00407~orig.jpg', color: '#b9674f', facts: { diameter: '6,779 km', mass: '6.39 × 10²³ kg', gravity: '3.7 m/s²', temperature: '-63°C avg.', distance: '227.9M km', day: '24.6 hours', year: '687 days', moons: '2' }, description: 'Known as the Red Planet, Mars is a cold desert world marked by ancient rivers, immense volcanoes and a restless history.', composition: 'Its rocky crust is rich in iron minerals that oxidize, giving the planet its rust-red color.', atmosphere: 'A thin carbon dioxide atmosphere offers little insulation. Dust storms can grow to cover the entire planet.', moons: 'Mars has two small, irregular moons: Phobos and Deimos.' },
  { id: 'jupiter', name: 'Jupiter', category: 'Planets', type: 'Gas giant', meta: '778.5M km from Sun', image: imageBase + 'PIA02884/PIA02884~orig.jpg', color: '#d2a375', facts: { diameter: '139,820 km', mass: '1.90 × 10²⁷ kg', gravity: '24.8 m/s²', temperature: '-110°C avg.', distance: '778.5M km', day: '9.9 hours', year: '11.86 years', moons: '95+' }, description: 'The largest world in our Solar System, Jupiter is a swirling sphere of hydrogen, storms and colossal magnetic fields.', composition: 'Hydrogen and helium dominate its atmosphere, transitioning into exotic metallic hydrogen deep inside.', atmosphere: 'Striped cloud bands, lightning and the Great Red Spot churn through its turbulent atmosphere.', moons: 'Its four largest moons, Io, Europa, Ganymede and Callisto, form a miniature planetary system.' },
  { id: 'saturn', name: 'Saturn', category: 'Planets', type: 'Gas giant', meta: '1.43B km from Sun', image: imageBase + 'PIA01364/PIA01364~orig.jpg', color: '#d6bb86', facts: { diameter: '116,460 km', mass: '5.68 × 10²⁶ kg', gravity: '10.4 m/s²', temperature: '-140°C avg.', distance: '1.43B km', day: '10.7 hours', year: '29.45 years', moons: '140+' }, description: 'Saturn’s bright rings make it the Solar System’s most recognizable planet, but the rings are only a thin layer of ice and rock.', composition: 'A gas giant composed mostly of hydrogen and helium, with a dense core of rock and ice.', atmosphere: 'Pale bands, powerful jet streams and a strange hexagonal storm crown its north pole.', moons: 'Titan, with its thick atmosphere and methane lakes, is one of the most intriguing moons known.' },
  { id: 'sun', name: 'The Sun', category: 'Stars', type: 'G-type star', meta: 'Our home star', image: imageBase + 'PIA18033/PIA18033~orig.jpg', color: '#f1bf65' },
  { id: 'europa', name: 'Europa', category: 'Moons', type: 'Icy moon', meta: 'Moon of Jupiter', image: imageBase + 'PIA19048/PIA19048~orig.jpg', color: '#d7c4a1' },
  { id: 'andromeda', name: 'Andromeda Galaxy', category: 'Galaxies', type: 'Spiral galaxy', meta: '2.5M light-years away', image: imageBase + 'PIA12348/PIA12348~orig.jpg', color: '#b692d1' },
  { id: 'orion', name: 'Orion Nebula', category: 'Nebulae', type: 'Stellar nursery', meta: '1,344 light-years away', image: imageBase + 'PIA01322/PIA01322~orig.jpg', color: '#df8eaa' },
  { id: 'sagittarius', name: 'Sagittarius A*', category: 'Black Holes', type: 'Supermassive black hole', meta: '26,000 light-years away', image: imageBase + 'PIA16695/PIA16695~orig.jpg', color: '#ba8ade' }
];
const categories = [
  ['Planets', 'Explore worlds near and far', '◉', '#a6e5ce'], ['Moons', 'Small worlds, big mysteries', '◐', '#d7c4a1'], ['Stars', 'The engines of the cosmos', '✦', '#f7d98c'], ['Galaxies', 'Cities of billions of stars', '◎', '#b692d1'], ['Black Holes', 'Where space folds in on itself', '◌', '#d99ce7'], ['Comets', 'Ancient ice on long orbits', '☄', '#85d7df'], ['Asteroids', 'Rocky time capsules', '◇', '#b9a68a'], ['Nebulae', 'Where stars are born', '✧', '#df8eaa']
];
const app = document.querySelector('#app');
let activeFilter = 'Planets';
let favoriteIds = JSON.parse(localStorage.getItem('cosmoverse-favorites') || '[]');
let userProfile = JSON.parse(localStorage.getItem('cosmoverse-profile') || 'null');
const gameState = { score: 0, integrity: 100, impacts: 0, chain: 0, minerals: [], tool: 'scan' };
const mineralPool = [{ name: 'Lunar ice', color: '#a7e8ef', value: 80 }, { name: 'Aurora crystal', color: '#d9a8ff', value: 150 }, { name: 'Iron-9 ore', color: '#ff9b7b', value: 110 }, { name: 'Stardust', color: '#f7d98c', value: 210 }];
const getObject = id => objects.find(item => item.id === id);
const objectImage = item => item.image || '';
function nav(route) { document.querySelectorAll('[data-route]').forEach(link => link.classList.toggle('active', link.dataset.route === route)); }
function layout(content) { app.innerHTML = `<div class="page">${content}</div>`; app.focus({ preventScroll: true }); }
function categoryCards() { return categories.map(([name, desc, symbol, color]) => `<button class="category-card" style="--card-color:${color};--card-glow:${color}1a" data-filter="${name}"><span class="category-symbol">${symbol}</span><h3>${name}</h3><p>${desc}</p></button>`).join(''); }
function objectCard(item) { return `<article class="object-card" data-object="${item.id}"><div class="object-image" style="background:linear-gradient(140deg,${item.color}77,#0c192c)">${objectImage(item) ? `<img loading="lazy" src="${objectImage(item)}" alt="${item.name}" />` : ''}</div><div class="object-body"><h3>${item.name}</h3><p>${item.type}</p><div class="object-meta"><span>${item.category}</span><span>${item.meta}</span></div></div></article>`; }
function home() { nav('home'); layout(`<section class="hero"><div class="hero-copy"><p class="eyebrow">A field guide to everything</p><h1>Explore the <em>universe.</em></h1><p>From the smallest asteroid to the largest galaxy, discover the incredible objects, mysteries and phenomena that make up our universe.</p><div class="hero-actions"><a href="#explore" data-route="explore" class="primary-button">Explore the universe <span class="button-arrow">→</span></a><button class="ghost-button" id="random-home">Random discovery <span>✦</span></button></div></div><div class="hero-visual" aria-label="Illustration of a planet moving through orbit"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="planet-art"></div><span class="hero-caption">Somewhere in the Milky Way · 01</span></div></section><section class="stats-row"><div class="stat"><strong>8</strong><span>planets in our system</span></div><div class="stat"><strong>200B+</strong><span>galaxies in the observable universe</span></div><div class="stat"><strong>13.8B</strong><span>years since the Big Bang</span></div><div class="stat"><strong>∞</strong><span>questions waiting for you</span></div></section><section><div class="section-heading"><div><p class="eyebrow">Start anywhere</p><h2>Choose your orbit</h2></div><a class="text-link" href="#explore" data-route="explore">View all objects →</a></div><div class="category-grid">${categoryCards()}</div></section>`); }
function explore() { nav('explore'); const filtered = objects.filter(item => item.category === activeFilter); layout(`<div class="section-heading"><div><p class="eyebrow">Object library</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">Explore the cosmos.</h1><p>Every object is a doorway to a bigger story.</p></div></div><div class="section-line"></div><div class="explore-layout"><aside><p class="eyebrow">Browse by type</p><div class="filter-list">${['Planets','Moons','Stars','Galaxies','Black Holes','Nebulae'].map(name => `<button class="${activeFilter === name ? 'active' : ''}" data-filter="${name}">${name}</button>`).join('')}</div></aside><section><div class="section-heading" style="margin-top:0"><h2 style="font-size:22px">${activeFilter}</h2><p>${filtered.length} objects indexed</p></div><div class="object-grid">${filtered.length ? filtered.map(objectCard).join('') : '<p class="empty-state">New objects are being charted here. Try another orbit.</p>'}</div></section></div>`); }
function solarSystem() { nav('solar-system'); const planets = [['mercury','#9f9d98','12%','14px'],['venus','#d99d61','22%','18px'],['earth','#69b8c8','32%','18px'],['mars','#b9624c','43%','15px'],['jupiter','#d1a27a','57%','38px'],['saturn','#d5bd87','70%','32px'],['uranus','#8bd2cf','81%','24px'],['neptune','#5679bd','90%','24px']]; layout(`<div class="section-heading"><div><p class="eyebrow">Our neighbourhood</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">The Solar System</h1><p id="scale-description">Distances are compressed so the whole family fits in one view.</p></div><div class="solar-controls" role="group" aria-label="Solar System scale"><button class="active" data-scale="educational" aria-pressed="true">Educational scale</button><button data-scale="realistic" aria-pressed="false">Realistic scale</button></div></div><div class="solar-wrap" data-scale="educational"><div class="solar-orbits"><div class="sun" title="The Sun"></div>${planets.map(([id,color,pos,size], index) => `<div class="s-orbit" style="--orbit:${20 + index * 10}%;--angle:${index % 2 ? 16 : -12}deg"><button class="s-planet" style="--planet:${color};--pos:${pos};--size:${size}" data-object="${id}" aria-label="View ${id}"></button></div>`).join('')}</div><div class="solar-legend"><span><i class="legend-dot" style="background:#e4a943"></i>Star</span><span><i class="legend-dot"></i>Orbital path</span><span>Click a planet to learn more</span></div></div>`); }
function game() { nav('game'); Object.assign(gameState, { score: 0, integrity: 100, impacts: 0, chain: 0, minerals: [], tool: 'scan' }); layout(`<div class="section-heading game-heading"><div><p class="eyebrow">Playful science simulator</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">Planet Pop Lab.</h1><p>Recover all four samples before the world loses integrity. Choose your risk.</p></div><div class="game-badge">SIMULATION · SECTOR 07</div></div><section class="game-layout"><div class="game-stage"><div class="game-stars"></div><div class="satellite satellite-one">◇</div><div class="satellite satellite-two">✦</div><div class="game-orbit game-orbit-one"></div><div class="game-orbit game-orbit-two"></div><button class="game-planet" id="game-planet" aria-label="Planet target"><span class="planet-ring"></span></button><div class="game-readout"><span>PLANET INTEGRITY</span><strong><b id="game-integrity">100</b>%</strong><small id="game-chain">CHAIN READY</small></div></div><aside class="game-console"><div class="console-top"><div><p class="eyebrow">Mission console</p><h2>Choose your instrument</h2></div><div class="score-orb"><span>score</span><strong id="game-score">0</strong></div></div><div class="tool-grid"><button class="tool-button active" data-game-tool="scan"><span>⌁</span><strong>Scan pulse</strong><small>Find a hidden pocket</small></button><button class="tool-button" data-game-tool="pick"><span>✧</span><strong>Plasma pick</strong><small>Gentle mineral mining</small></button><button class="tool-button" data-game-tool="hammer"><span>✹</span><strong>Gravity hammer</strong><small>Big impact, rare finds</small></button><button class="tool-button" data-game-tool="comet"><span>☄</span><strong>Comet slingshot</strong><small>Wild orbital collision</small></button></div><button class="primary-button game-action" id="game-action">Deploy scan pulse <span>→</span></button><div class="game-log" id="game-log">Satellite link stable. Scan first, then choose how hard to break the surface.</div><div class="mineral-panel"><div class="mineral-title"><span>Recovered samples</span><span id="mineral-count">0 / 4</span></div><div id="mineral-list" class="mineral-list"><span class="mineral-empty">No samples yet. Scan the surface.</span></div></div></aside></section>`); }
function detail(id) { const item = getObject(id) || getObject('mars'); nav('explore'); const f = item.facts || {}; layout(`<a class="text-link" href="#explore" data-route="explore">← Back to explorer</a><section class="detail-hero"><div class="detail-planet-stage"><div class="detail-planet ${item.id}" style="--planet-image:url('${objectImage(item)}')"></div></div><div class="detail-copy"><p class="detail-tag">${item.category} · ${item.type}</p><h1>${item.name}</h1><p class="lead">${item.description}</p><button class="primary-button favorite-button" data-favorite="${item.id}">${favoriteIds.includes(item.id) ? 'Saved to My Cosmos' : 'Save to My Cosmos'} <span>♡</span></button></div></section>${item.facts ? `<section><div class="section-heading"><div><p class="eyebrow">A quick read</p><h2>Key measurements</h2></div></div><div class="fact-grid">${Object.entries(f).map(([key, value]) => `<div class="fact"><span>${key}</span><strong>${value}</strong></div>`).join('')}</div><div class="detail-columns"><div><h2>Composition</h2><p>${item.composition}</p><h2>Atmosphere</h2><p>${item.atmosphere}</p></div><div><h2>Its moons</h2><p>${item.moons}</p><h2>Keep exploring</h2><p>Every measurement is an invitation to ask a better question. Browse another world, compare it with home, or let the universe choose your next stop.</p><a class="text-link" href="#compare" data-route="compare">Open cosmic comparison →</a></div></div></section>` : ''}`); }
function login() { nav('login'); layout(`<section class="login-layout"><div class="login-visual"><div class="login-orbit login-orbit-one"></div><div class="login-orbit login-orbit-two"></div><div class="login-earth"></div><p class="login-coordinates">34.0522° N · 118.2437° W<br><span>CosmoVerse Observatory</span></p></div><div class="login-panel"><p class="eyebrow">Private observatory access</p><h1>Return to your <em>orbit.</em></h1><p class="login-intro">Sign in to save discoveries, build your personal cosmos and continue exactly where you left off.</p><form id="login-form"><label>Email address<input type="email" name="email" placeholder="you@example.com" required /></label><label>Password<input type="password" name="password" placeholder="Enter your password" required /></label><div class="login-options"><label class="checkbox-label"><input type="checkbox" /> Remember me</label><button type="button" class="text-link forgot-button" data-forgot>Forgot password?</button></div><button class="primary-button login-submit" type="submit">Enter CosmoVerse <span class="button-arrow">→</span></button></form><div class="login-switch"><span>New to the observatory?</span><a href="#signup" data-route="signup" class="ghost-button login-create-button">Create an account <span>→</span></a></div><p class="login-note">Demo access is local to this browser. No credentials leave your device.</p></div></section>`); }
function signup() { nav('signup'); layout(`<section class="login-layout signup-layout"><div class="login-visual"><div class="login-orbit login-orbit-one"></div><div class="login-orbit login-orbit-two"></div><div class="login-earth signup-orb"></div><p class="login-coordinates">NEW EXPLORER PROTOCOL<br><span>CosmoVerse Observatory</span></p></div><div class="login-panel"><p class="eyebrow">Create your explorer profile</p><h1>Claim your <em>orbit.</em></h1><p class="login-intro">Choose a callsign and avatar. Your profile stays local in this demo so your personal cosmos is ready on this device.</p><form id="signup-form"><label>Explorer name<input type="text" name="name" placeholder="Your callsign" minlength="2" required /></label><label>Email address<input type="email" name="email" placeholder="you@example.com" required /></label><label>Choose an avatar<select name="avatar" required><option value="astronaut">Astronaut</option><option value="planet">Planet</option><option value="comet">Comet</option><option value="star">Star</option></select></label><label>Password<input type="password" name="password" placeholder="Create a password" minlength="4" required /></label><button class="primary-button login-submit" type="submit">Create explorer profile <span class="button-arrow">→</span></button></form><p class="login-switch">Already have an orbit? <a href="#login" data-route="login" class="text-link">Sign in</a></p><p class="login-note">Demo profile data is stored only in this browser.</p></div></section>`); }
function discover() { nav('discover'); const item = objects[Math.floor(Math.random() * objects.length)]; layout(`<div class="section-heading"><div><p class="eyebrow">Take me somewhere</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">A new signal.</h1><p>The universe is very good at surprising us.</p></div><button class="ghost-button" id="random-again">Another discovery <span>↻</span></button></div><div class="discovery-card"><div class="discovery-image" style="background-image:linear-gradient(160deg,${item.color}55,rgba(7,17,31,.5)),url('${objectImage(item)}')"></div><div class="discovery-copy"><p class="eyebrow">✦ Today's discovery</p><h1>${item.name}</h1><p>${item.description}</p><div class="discovery-facts"><div><strong>${item.category}</strong><span>category</span></div><div><strong>${item.meta.split(' ')[0]}</strong><span>first signal</span></div></div><button class="primary-button" data-object="${item.id}">Explore this object <span class="button-arrow">→</span></button></div></div>`); }
function compare() { nav('compare'); const first = getObject('earth'); const second = getObject('mars'); layout(`<div class="section-heading"><div><p class="eyebrow">See the difference</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">Cosmic comparison.</h1><p>Put two worlds side by side and let scale tell the story.</p></div></div><div class="compare-selects"><select class="select-object" id="compare-a">${objects.filter(item => item.facts).map(item => `<option value="${item.id}" ${item.id === first.id ? 'selected' : ''}>${item.name}</option>`).join('')}</select><select class="select-object" id="compare-b">${objects.filter(item => item.facts).map(item => `<option value="${item.id}" ${item.id === second.id ? 'selected' : ''}>${item.name}</option>`).join('')}</select></div><div id="compare-content">${compareTable(first, second)}</div>`); }
function compareTable(a, b) { const keys = [['diameter','Diameter'],['mass','Mass'],['gravity','Gravity'],['temperature','Temperature'],['day','Length of day'],['year','Length of year'],['moons','Moons']]; return `<div class="compare-table"><div class="compare-row"><span>Object</span><div class="compare-value"><strong>${a.name}</strong><div class="bar"><i style="--width:82%"></i></div></div><div class="compare-value"><strong>${b.name}</strong><div class="bar coral"><i style="--width:51%"></i></div></div></div>${keys.map(([key,label], index) => `<div class="compare-row"><span>${label}</span><div class="compare-value"><strong>${a.facts[key]}</strong><div class="bar"><i style="--width:${Math.max(18, 84 - index * 7)}%"></i></div></div><div class="compare-value"><strong>${b.facts[key]}</strong><div class="bar coral"><i style="--width:${Math.max(13, 53 - index * 5)}%"></i></div></div></div>`).join('')}</div>`; }
function favorites() { nav('favorites'); const saved = objects.filter(item => favoriteIds.includes(item.id)); const profile = userProfile ? `<section class="profile-strip"><div class="profile-avatar ${userProfile.avatar}">${avatarSymbol(userProfile.avatar)}</div><div><p class="eyebrow">Explorer profile</p><h2>${userProfile.name}</h2><p>${userProfile.email} · ${saved.length} saved worlds</p></div><a class="ghost-button" href="#login" data-route="login">Switch profile</a></section>` : `<section class="profile-strip profile-empty"><div class="profile-avatar astronaut">✦</div><div><p class="eyebrow">Your observatory profile</p><h2>Make this cosmos yours.</h2><p>Sign in or create an explorer profile to save discoveries.</p></div><a class="primary-button" href="#signup" data-route="signup">Create profile <span>→</span></a></section>`; layout(`<div class="section-heading"><div><p class="eyebrow">Your collection</p><h1 style="font-size:clamp(44px,6vw,72px);margin-bottom:8px">My Cosmos.</h1><p>A small constellation of things you want to remember.</p></div></div>${profile}${saved.length ? `<div class="object-grid">${saved.map(objectCard).join('')}</div>` : '<div class="empty-state">You have not saved anything yet. Open an object and tap “Save to My Cosmos” to start your collection.</div>'}`); }
function avatarSymbol(type) { return { astronaut: '✦', planet: '◉', comet: '☄', star: '✧' }[type] || '✦'; }
function updateAvatar() { const avatar = document.querySelector('.avatar-button'); if (!avatar) return; avatar.className = `avatar-button ${userProfile?.avatar || 'guest'}`; avatar.textContent = userProfile ? avatarSymbol(userProfile.avatar) : 'CV'; avatar.setAttribute('aria-label', userProfile ? `Open ${userProfile.name} profile` : 'Open sign in'); }
function showToast(message) { const toast = document.querySelector('#toast'); toast.textContent = message; toast.classList.add('show'); setTimeout(() => toast.classList.remove('show'), 2500); }
function solarSystem() { nav('solar-system'); const planets = [['mercury','#9f9d98','12%','36px','Mercury','Rocky · 57.9M km'],['venus','#d99d61','23%','42px','Venus','Rocky · 108.2M km'],['earth','#69b8c8','34%','43px','Earth','Rocky · 149.6M km'],['mars','#b9624c','45%','39px','Mars','Rocky · 227.9M km'],['jupiter','#d1a27a','58%','67px','Jupiter','Gas giant · 778.5M km'],['saturn','#d5bd87','70%','60px','Saturn','Gas giant · 1.43B km'],['uranus','#8bd2cf','81%','50px','Uranus','Ice giant · 2.87B km'],['neptune','#5679bd','91%','50px','Neptune','Ice giant · 4.5B km']]; const selected = getObject('earth'); layout(`<div class="system-header"><div><p class="eyebrow">Mission control · 01</p><h1>The Solar System</h1><p id="scale-description">Distances are compressed for a readable tour of our neighborhood.</p></div><div class="solar-controls" role="group" aria-label="Solar System scale"><button class="active" data-scale="educational" aria-pressed="true">Tour scale</button><button data-scale="realistic" aria-pressed="false">Distance view</button></div></div><section class="system-layout"><div class="solar-wrap planetarium" data-scale="educational" data-view="horizontal" data-zoom="1"><div class="solar-corner">LIVE 3D ORBITAL MAP <span>· 8 WORLDS</span></div><div class="system-view-controls" role="group" aria-label="3D map controls"><button class="active" data-system-view="horizontal">Horizontal</button><button data-system-view="vertical">Vertical</button><button data-system-zoom="out">−</button><button data-system-zoom="in">+</button></div><div class="solar-orbits"><div class="sun" title="The Sun"></div>${planets.map(([id,color,pos,size,name,meta], index) => `<div class="s-orbit" style="--orbit:${19 + index * 10}%;--angle:${index % 2 ? 16 : -12}deg"><button class="s-planet" style="--planet:${color};--pos:${pos};--size:${size}" data-system-object="${id}" aria-label="View ${name}"><span>${name}</span></button></div>`).join('')}</div><div class="solar-legend"><span><i class="legend-dot" style="background:#e4a943"></i>Star</span><span><i class="legend-dot"></i>Orbit path</span><span>Tap a world to inspect</span></div></div><aside class="system-inspector"><p class="eyebrow">Selected world</p><div class="inspector-planet earth"></div><h2 id="system-name">${selected.name}</h2><p id="system-meta">${selected.type} · ${selected.meta}</p><div class="inspector-stats"><span><b id="system-diameter">${selected.facts.diameter}</b> diameter</span><span><b id="system-moons">${selected.facts.moons}</b> moons</span><span><b id="system-day">${selected.facts.day}</b> day length</span></div><figure class="satellite-feed"><img id="system-image" src="${objectImage(selected)}" alt="Satellite image of ${selected.name}" /><figcaption id="system-caption">Satellite image · ${selected.name}</figcaption></figure><a class="primary-button" id="system-open" href="#planet/earth">Open world file <span>→</span></a><p class="inspector-note">3D view is educational. Use the orientation and zoom controls to inspect the system.</p></aside></section>`); }
function route() { const hash = location.hash.slice(1) || 'home'; if (hash.startsWith('planet/')) return detail(hash.split('/')[1]); if (hash === 'explore') return explore(); if (hash === 'solar-system') return solarSystem(); if (hash === 'discover') return discover(); if (hash === 'compare') return compare(); if (hash === 'favorites') return favorites(); if (hash === 'login') return login(); if (hash === 'signup') return signup(); if (hash === 'game') return game(); return home(); }
document.addEventListener('click', event => { const scaleButton = event.target.closest('[data-scale]'); if (scaleButton) { const controls = scaleButton.closest('.solar-controls'); const canvas = document.querySelector('.solar-wrap'); const description = document.querySelector('#scale-description'); if (!controls || !canvas || !description) return; controls.querySelectorAll('[data-scale]').forEach(button => { const selected = button === scaleButton; button.classList.toggle('active', selected); button.setAttribute('aria-pressed', selected); }); canvas.dataset.scale = scaleButton.dataset.scale; description.textContent = scaleButton.dataset.scale === 'realistic' ? 'Distances are shown with stronger separation to hint at the true emptiness of space.' : 'Distances are compressed so the whole family fits in one view.'; return; } const routeLink = event.target.closest('[data-route]'); if (routeLink) { event.preventDefault(); location.hash = routeLink.dataset.route; return; } const filter = event.target.closest('[data-filter]'); if (filter) { activeFilter = filter.dataset.filter; if (location.hash !== '#explore') location.hash = 'explore'; else explore(); return; } const object = event.target.closest('[data-object]'); if (object) { location.hash = `planet/${object.dataset.object}`; return; } const favorite = event.target.closest('[data-favorite]'); if (favorite) { const id = favorite.dataset.favorite; favoriteIds = favoriteIds.includes(id) ? favoriteIds.filter(saved => saved !== id) : [...favoriteIds, id]; localStorage.setItem('cosmoverse-favorites', JSON.stringify(favoriteIds)); showToast(favoriteIds.includes(id) ? 'Added to My Cosmos' : 'Removed from My Cosmos'); detail(id); return; } if (event.target.closest('#random-home') || event.target.closest('#random-again')) { location.hash = 'discover'; return; } if (event.target.closest('[data-close-search]')) closeSearch(); });
document.addEventListener('click', event => { const world = event.target.closest('[data-system-object]'); if (!world) return; const item = getObject(world.dataset.systemObject); if (!item || !item.facts) return; document.querySelectorAll('[data-system-object]').forEach(button => button.classList.toggle('selected', button === world)); const inspector = document.querySelector('.inspector-planet'); inspector.className = `inspector-planet ${item.id}`; document.querySelector('#system-name').textContent = item.name; document.querySelector('#system-meta').textContent = `${item.type} · ${item.meta}`; document.querySelector('#system-diameter').textContent = item.facts.diameter; document.querySelector('#system-moons').textContent = item.facts.moons; document.querySelector('#system-day').textContent = item.facts.day; document.querySelector('#system-open').href = `#planet/${item.id}`; const image = document.querySelector('#system-image'); if (image && item.image) { image.src = item.image; image.alt = `Satellite image of ${item.name}`; document.querySelector('#system-caption').textContent = `Satellite image · ${item.name}`; } });
document.addEventListener('pointerdown', event => { const canvas = event.target.closest('.planetarium'); if (!canvas || event.target.closest('.system-view-controls')) return; const worlds = [...canvas.querySelectorAll('[data-system-object]')]; const nearest = worlds.map(world => { const rect = world.getBoundingClientRect(); return { world, distance: Math.hypot(event.clientX - (rect.left + rect.width / 2), event.clientY - (rect.top + rect.height / 2)) }; }).sort((a, b) => a.distance - b.distance)[0]; if (nearest && nearest.distance < 100) nearest.world.click(); });
document.addEventListener('click', event => { const viewButton = event.target.closest('[data-system-view]'); const zoomButton = event.target.closest('[data-system-zoom]'); const canvas = document.querySelector('.planetarium'); if (!canvas) return; if (viewButton) { canvas.dataset.view = viewButton.dataset.systemView; document.querySelectorAll('[data-system-view]').forEach(button => button.classList.toggle('active', button === viewButton)); return; } if (zoomButton) { const current = Number(canvas.dataset.zoom || 1); canvas.dataset.zoom = zoomButton.dataset.systemZoom === 'in' ? Math.min(3, current + 1) : Math.max(1, current - 1); return; } });
function openSearch() { const modal = document.querySelector('#search-modal'); modal.classList.add('open'); modal.setAttribute('aria-hidden', 'false'); setTimeout(() => document.querySelector('#global-search').focus(), 50); }
function closeSearch() { const modal = document.querySelector('#search-modal'); modal.classList.remove('open'); modal.setAttribute('aria-hidden', 'true'); }
document.querySelector('#search-toggle').addEventListener('click', openSearch);
document.querySelector('#global-search').addEventListener('input', event => { const query = event.target.value.toLowerCase().trim(); const results = objects.filter(item => `${item.name} ${item.category} ${item.type} ${item.description || ''}`.toLowerCase().includes(query)); document.querySelector('#search-results').innerHTML = query ? (results.length ? results.map(item => `<div class="search-result" data-object="${item.id}"><div><h3>${item.name}</h3><p>${item.category} · ${item.type}</p></div><span class="text-link">Open →</span></div>`).join('') : '<p class="search-hint">No object found. Try another signal.</p>') : '<p class="search-hint">Search planets, stars, galaxies, phenomena, missions and more.</p>'; });
document.addEventListener('change', event => { if (event.target.matches('#compare-a, #compare-b')) { const a = getObject(document.querySelector('#compare-a').value); const b = getObject(document.querySelector('#compare-b').value); document.querySelector('#compare-content').innerHTML = compareTable(a, b); } });
document.addEventListener('submit', event => { if (event.target.matches('#login-form')) { event.preventDefault(); const email = new FormData(event.target).get('email'); userProfile = userProfile || { name: email.split('@')[0], email, avatar: 'astronaut' }; localStorage.setItem('cosmoverse-profile', JSON.stringify(userProfile)); localStorage.setItem('cosmoverse-signed-in', 'true'); updateAvatar(); showToast(`Welcome back, ${userProfile.name}`); location.hash = 'favorites'; } if (event.target.matches('#signup-form')) { event.preventDefault(); const data = new FormData(event.target); userProfile = { name: data.get('name'), email: data.get('email'), avatar: data.get('avatar') }; localStorage.setItem('cosmoverse-profile', JSON.stringify(userProfile)); localStorage.setItem('cosmoverse-signed-in', 'true'); updateAvatar(); showToast(`Welcome aboard, ${userProfile.name}`); location.hash = 'favorites'; } });
function runGameTool() { const tool = gameState.tool; const planet = document.querySelector('#game-planet'); const log = document.querySelector('#game-log'); if (!planet || !log) return; if (tool === 'scan') { const target = mineralPool[gameState.minerals.length % mineralPool.length]; gameState.score += 25; gameState.chain = 0; log.textContent = `Scan complete. ${target.name} signal found beneath the surface.`; planet.classList.add('scan-pulse'); setTimeout(() => planet.classList.remove('scan-pulse'), 650); } else { const impact = tool === 'pick' ? 8 : tool === 'hammer' ? 18 : 27; gameState.integrity = Math.max(0, gameState.integrity - impact); gameState.impacts += 1; gameState.chain += 1; const bonus = gameState.chain * 20; const found = mineralPool[(gameState.impacts - 1) % mineralPool.length]; if (!gameState.minerals.some(sample => sample.name === found.name)) { gameState.minerals.push(found); gameState.score += found.value + bonus; log.textContent = `${found.name} recovered. Combo bonus +${bonus}. The ${tool} made a ${impact}% impact.`; } else { gameState.score += 20 + bonus; log.textContent = `Surface cracked ${impact}%. Combo bonus +${bonus}. More ${found.name} detected.`; } const mark = document.createElement('i'); mark.className = 'impact-mark'; mark.style.left = `${28 + Math.random() * 44}%`; mark.style.top = `${30 + Math.random() * 40}%`; planet.appendChild(mark); if (gameState.integrity === 0) { planet.classList.add('planet-popped'); log.textContent = 'Planet popped! Every sample recovered. Mission complete.'; document.querySelector('#game-action').textContent = 'Mission complete'; } } document.querySelector('#game-score').textContent = gameState.score; document.querySelector('#game-integrity').textContent = gameState.integrity; document.querySelector('#game-chain').textContent = gameState.chain > 1 ? `${gameState.chain}x EXTRACTION CHAIN` : 'CHAIN READY'; document.querySelector('#mineral-count').textContent = `${gameState.minerals.length} / ${mineralPool.length}`; document.querySelector('#mineral-list').innerHTML = gameState.minerals.length ? gameState.minerals.map(sample => `<span class="mineral-chip" style="--mineral:${sample.color}">${sample.name}<b>+${sample.value}</b></span>`).join('') : '<span class="mineral-empty">No samples yet. Scan the surface.</span>'; }
document.addEventListener('click', event => { const toolButton = event.target.closest('[data-game-tool]'); if (toolButton) { document.querySelectorAll('[data-game-tool]').forEach(button => button.classList.toggle('active', button === toolButton)); gameState.tool = toolButton.dataset.gameTool; const labels = { scan: 'Deploy scan pulse', pick: 'Fire plasma pick', hammer: 'Swing gravity hammer', comet: 'Launch comet slingshot' }; document.querySelector('#game-action').innerHTML = `${labels[gameState.tool]} <span>→</span>`; return; } if (event.target.closest('#game-action')) runGameTool(); });
document.addEventListener('click', event => { if (event.target.closest('[data-forgot]')) showToast('Password reset is available in the connected account version.'); });
window.addEventListener('hashchange', route); route(); updateAvatar();
document.addEventListener('click', event => { if (event.target.closest('.search-result[data-object]')) closeSearch(); });
